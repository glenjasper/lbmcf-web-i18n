from django.urls import path
from . import views
'''
urlpatterns = [
    path(
        route = '<slug:code>/',
        view = views.PartnershipPlaceView.as_view(),
        name = 'url_partnerships'
    ),
]
'''
